# Supabase configuration (Required)
# Get these from your Supabase project settings
VITE_SUPABASE_URL="YOUR_SUPABASE_URL_HERE"
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY_HERE"

# Email (SMTP) configuration (Optional - Required for email features like password reset)
# Get these from your email provider (e.g., SendGrid, Mailgun, etc.)
# Ensure secure connection (TLS/SSL) is used.
SMTP_HOST="YOUR_SMTP_HOST"
SMTP_PORT="YOUR_SMTP_PORT" # e.g., 587 or 465
SMTP_USER="YOUR_SMTP_USERNAME"
SMTP_PASS="YOUR_SMTP_PASSWORD_OR_APP_KEY"
